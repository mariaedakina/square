#ifndef PROVERKA_H
#define PROVERKA_H

#include "solver.h"

/**
 * @file proverka.h
 * @brief Заголовочный файл для проверки тестов.
 *
 * Этот файл содержит декларации функций, связанных с проверкой
 * тестовых данных и запуском тестов.
 */

/**
 * @brief Проверяет результаты решения квадратного уравнения с помощью тестовых данных.
 *
 * Функция принимает тестовые данные, решает квадратное уравнение и проверяет,
 * соответствуют ли полученные корни ожидаемым значениям. Если результаты не совпадают,
 * выводит сообщение об ошибке.
 *
 * @param r Структура Test_Date, содержащая данные для тестирования:
 *          - nTest: номер теста
 *          - koefs: коэффициенты квадратного уравнения (a, b, c)
 *          - right_x1: ожидаемый первый корень
 *          - right_x2: ожидаемый второй корень
 *          - right_nRoots: ожидаемое количество корней
 *
 * @return 0, если тест пройден успешно; 1, если есть ошибка.
 */
int Proverka_Testami(Test_Date r);

/**
 * @brief Запускает набор тестов для проверки решения квадратных уравнений.
 *
 * Функция определяет массив тестовых данных и вызывает функцию проверки для каждого
 * из них. В случае ошибки выводит номер теста, который не прошел проверку.
 *
 * @note В тестах используются заранее определенные значения коэффициентов и ожидаемых корней.
 */
void RUN_TESTS();

#endif // PROVERKA_H

